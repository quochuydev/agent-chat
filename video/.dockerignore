# Heavy model venvs (Apple/MLX-specific) — never needed by the connector image.
.venv
.venv-api
__pycache__/
*.pyc
.pytest_cache

# Runtime state — lives in mounted volumes, not the image.
jobs.db
api/runs/

# Stray local artifacts.
*.wav
.DS_Store
