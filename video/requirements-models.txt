# Heavy model deps for local generation — installed into video/.venv by setup.sh.
# Apple-Silicon only (MLX). These are NEVER imported by the connector API; they run
# per-job as a subprocess (see api/tasks.py). Kept separate from requirements-api.txt.
#
# Version floors match video/README.md. For a fully reproducible pin, after a known-good
# install run:  .venv/bin/pip freeze > requirements-models.lock.txt  and install from that.
kokoro>=0.9.2
soundfile>=0.12
mflux>=0.6.2
